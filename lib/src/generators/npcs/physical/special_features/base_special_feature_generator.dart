import '../../../base/list_item_generator.dart';
import 'special_features_data.dart';

/// A class that generates a special feature
class BaseSpecialFeatureGenerator extends ListItemGenerator<String> {
  BaseSpecialFeatureGenerator() : super(genericSpecialFeatures);
}
