import '../../../base/list_item_generator.dart';
import 'build_data.dart';

/// A class that generates a generic build
class BaseBuildGenerator extends ListItemGenerator<String> {
  BaseBuildGenerator() : super(genericBuild);
}
