import '../../../base/list_item_generator.dart';
import 'build_data.dart';

/// A class that generates a light build
class LightBuildGenerator extends ListItemGenerator<String> {
  LightBuildGenerator() : super(lightBuild);
}
