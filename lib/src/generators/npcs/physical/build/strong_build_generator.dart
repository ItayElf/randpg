import '../../../base/list_item_generator.dart';
import 'build_data.dart';

/// A class that generates a strong build
class StrongBuildGenerator extends ListItemGenerator<String> {
  StrongBuildGenerator() : super(strongBuild);
}
